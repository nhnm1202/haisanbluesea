<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <title>Kết quả tìm kiếm - <%= keyword %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/style.css" />
    <script
      src="https://kit.fontawesome.com/f542258c79.js"
      crossorigin="anonymous"
    ></script>
  </head>
  <body>
    <%- include('partials/header') %>

    <div class="container my-5">
      <h2 class="mb-4">Kết quả cho: "<%= keyword %>"</h2>

      <% if (products.length === 0) { %>
      <p>Không tìm thấy sản phẩm nào phù hợp.</p>
      <% } else { %>
      <div class="row">
        <% products.forEach(p => { %>
        <div class="col-md-3 mb-4">
          <div class="card h-100 shadow-sm">
            <img
              src="<%= p.image %>"
              class="card-img-top"
              alt="<%= p.name %>"
            />
            <div class="card-body d-flex flex-column">
              <h5 class="card-title text-center"><%= p.name %></h5>
              <p class="text-center">
                Xuất xứ: <%= p.origin %><br />
                <span class="text-danger fw-bold"><%= p.price %></span>
              </p>
              <div class="d-flex justify-content-center gap-3 mt-auto">
                <button
                  class="btn btn-info btn-sm btn-add-to-cart p-2 fs-4"
                  data-id="<%= p.id %>"
                  data-name="<%= p.name %>"
                  data-price="<%= p.price %>"
                  data-image="<%= p.image %>"
                >
                  <i class="fas fa-cart-plus me-2"></i>Thêm giỏ
                </button>
                <a
                  href="/product-detail/<%= p.id %>"
                  class="btn btn-warning btn-sm p-2 fs-4"
                  ><i class="fas fa-credit-card me-2"></i> Mua ngay</a
                >
              </div>
            </div>
          </div>
        </div>
        <% }) %>
      </div>
      <% } %>
    </div>

    <%- include('partials/footer') %>
    <script src="/js/add-cart.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
